'''
Design a data structure that supports the following operations, all in average O(1) time complexity:

insert(val): Inserts an item val into the collection. Returns True even if the item is already present.
remove(val): Removes one instance of item val from the collection. Returns True if the item was present and removed, False otherwise.
getRandom(): Returns a random element from the current collection. 
The probability of selecting a particular element must be proportional to the number of times it appears in the collection.

HashMap -> 
List: x y z y z y y

length of DLL -> n
(0, n-1) 

Insert(x) 
x: [Node x]
y: [Node y, Node y]
z: [Node z, Node z, Node z]
'''
